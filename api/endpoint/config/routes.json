{
  "routes": [
    {
      "method": "PUT",
      "path": "/user-update/:id",
      "handler": "endpoint.userUpdate",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/list/:module",
      "handler": "endpoint.list",
      "config": {
        "policies": []
      }
    }
  ]
}
